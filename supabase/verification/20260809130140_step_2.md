# Step 2 Supabase verification

Migration: `20260809130140_baseline_and_reconcile_schema`

Applied to production project `gmtdqeskyvdvyibccxwt` on 2026-08-09.

## Data preservation

Row counts before and after the migration were identical:

| Table | Rows |
| --- | ---: |
| admin_users | 1 |
| works | 5 |
| work_images | 0 |
| reviews | 4 |
| site_settings | 1 |
| queries | 0 |
| estimate_requests | 0 |

All five populated `short_description` and `long_description` values remained
intact. No validation rows remained after the rollback-only tests.

## Security and access

- RLS is enabled on all seven public tables.
- Anonymous visitors have read-only access to public site content.
- Anonymous visitors can insert queries and estimate requests.
- Authenticated active admins can create, update, and delete works, images, and reviews.
- Authenticated active admins can insert or update the singleton site settings row.
- Query and estimate rows are readable only by authenticated active admins.
- Required identity-sequence privileges are explicitly granted.

## Functional database tests

The following operations were executed inside transactions and rolled back:

- Public query submission
- Public estimate submission
- Admin work create, update, and delete
- Admin normalized work-image insert
- Admin review create, update, and delete
- Admin site-settings upsert

Both the public and admin policy test suites passed.

## Local verification

- Canonical migration transactional replay: passed
- Production build: passed
- Node test suite: 6/6 passed
- ESLint: passed

## Remaining advisor notices

Supabase Security Advisor reports that leaked-password protection is disabled.
This is an Auth account setting, not a schema fault, and is scheduled for the
security-hardening step.

Performance Advisor reports new low-traffic indexes as unused. They are retained
because the project has not yet accumulated representative production traffic.

