# Step 3 application contract verification

Verified on 2026-08-09 against Supabase production project
`gmtdqeskyvdvyibccxwt`.

## Canonical mappings

- Works read and write `short_description` and `long_description`.
- Work images read and write `secure_url`, `byte_size`, and `sort_order`.
- Reviews read and write `instagram_url`.
- Site settings read and write `instagram_url` and `tiktok_url`.
- Estimate submissions write `approximate_size` and `material_preference`.
- Queries use the canonical `name`, `phone`, `category`, and `message` fields.
- Numeric Supabase identity values are converted to strings only at the locked
  UI boundary and validated before writes or deletes.

The Supabase client is parameterized with types generated directly from the
production schema. Read queries select explicit canonical columns instead of
using `select('*')`.

## Defensive handling

- Editable text values are normalized without calling `.trim()` on undefined.
- Missing work-image dimensions or byte size produce a clear error before a
  database write.
- Admin login and restored sessions require an active `admin_users` row.
- Update and delete operations request the affected ID so a zero-row RLS or ID
  mismatch cannot be reported as success.

## Live read-only verification

- Works: 5 total, 0 invalid canonical rows
- Work images: 0 total, 0 invalid canonical rows
- Reviews: 4 total, 0 invalid canonical rows
- Singleton settings row: present with canonical social URL fields

## Local verification

- TypeScript production build: passed
- Vite production build: passed
- Node test suite: 7/7 passed
- ESLint: passed
- `git diff --check`: passed
- Locked public/admin surface regression test: passed

No component hierarchy, labels, controls, layout classes, or public/admin
navigation structure were changed in this step.
